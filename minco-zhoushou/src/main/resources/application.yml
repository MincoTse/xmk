server:
  port: 8061

spring:
  profiles:
    active: dev


mybatis-plus:
  type-aliases-package: com.minco.zhushou.entity
  mapper-locations: classpath*:mapper/*.xml
  global-config:
    db-config:
      table-prefix: tse_

