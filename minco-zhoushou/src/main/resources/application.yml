server:
  port: 8061
spring:
  profiles:
    active: dev
    redis:
      database: 3
      host: base-nonprod-slb-public.53zaixian.com
      port: 30019
      password: uTetAk8gOP3d
      timeout: 5000
      jedis:
        pool:
          max-active: 1000 # 连接池最大连接数（使用负值表示没有限制）
          max-idle: 10 # 连接池中的最大空闲连接
          min-idle: 0 # 连接池中的最小空闲连接
          max-wait: -1 # 连接池最大阻塞等待时间（使用负值表示没有限制）



mybatis-plus:
  type-aliases-package: com.minco.zhushou.entity
  mapper-locations: classpath*:mapper/*.xml
  global-config:
    db-config:
      table-prefix: tse_


qyx:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://sql-t01cn-rw.svc.53zaixian.com:3306/__db.name__?useUnicode=true&characterEncoding=utf8&allowMultiQueries=true&serverTimezone=GMT%2B8
    username: app_test
    password: quWxRxYv1GzB%1iy
  server:
    type: service
db:
  name:
    - db-name: member_center_0

